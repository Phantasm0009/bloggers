<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>How to Build a Todo Application With Python Flask, Heroku and Tailwind CSS</title>
      <link href="/2022/12/20/flask/"/>
      <url>/2022/12/20/flask/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/flask.webp" alt="$cover"></p><h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a><a href="#introduction"></a>Introduction</h2><p>Flask is called a <strong>MicroFramework</strong> because it gives you the basic tools you need in order to build a web application in <strong>Python</strong>.<br>With Flask you can build any kind of Web service or backend application.<br>If you’re beginning using Python for web development i suggest you to start with <strong>Django</strong>.</p><p>In this guide We will build a Todo application from Scratch with <strong>Tailwind</strong> A utility-first CSS framework for rapidly building custom designs.</p><h2 id="Requirements"><a href="#Requirements" class="headerlink" title="Requirements"></a><a href="#requirements"></a>Requirements</h2><p>To follow along with me make sure you’ve :</p><ul><li>python3</li><li>pip3</li><li>pipenv</li><li>Tailwind CDN</li></ul><h2 id="Application-Setup"><a href="#Application-Setup" class="headerlink" title="Application Setup"></a><a href="#application-setup"></a>Application Setup</h2><p>Create new folder and install the necessary tools.</p><p>Create a new folder  </p><pre><code>$ mkdir flask_tailwind_todo_app$ cd flask_tailwind_todo_app/...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Create a Virtual environment with <strong>pipenv</strong>  </p><pre><code>$ pipenv shell...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><pre><code>Creating a virtualenv for this project…Using /usr/bin/python3 (3.7.5) to create virtualenv…⠋Already using interpreter /usr/bin/python3Using base prefix &#39;/usr&#39;New python executable in /home/username/.local/share/virtualenvs/flask_tailwind_todo_app-4wPj_lFD/bin/python3Also creating executable in /home/username/.local/share/virtualenvs/flask_tailwind_todo_app-4wPj_lFD/bin/pythonInstalling setuptools, pip, wheel...done.Virtualenv location: /home/username/.local/share/virtualenvs/flask_tailwind_todo_app-4wPj_lFDCreating a Pipfile for this project…Spawning environment shell (/usr/bin/zsh). Use &#39;exit&#39; to leave.. /home/username/.local/share/virtualenvs/flask_tailwind_todo_app-4wPj_lFD/bin/activateusername@username-Latitude-7480 ~/projects/xarala/source-code/flask_tailwind_todo_app╰─$ . /home/username/.local/share/virtualenvs/flask_tailwind_todo_app-4wPj_lFD/bin/activate(flask_tailwind_todo_app-4wPj_lFD) username@username-Latitude-7480 ~/projects/xarala/source-code/flask_tailwind_todo_app╰─$</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>This command will create a new virtual environment and activate it.</p><p>You can learn more about <strong>Pipenv</strong> in this <a href="https://www.ousseynoudiop.com/python-how-to-setup-your-virtualenv-correctly/python-how-to-setup-your-virtualenv-correctly/">Post</a></p><p>Install <strong>flask</strong>, <strong>sqlalchemy</strong> and <strong>gunicorn</strong>  </p><pre><code>$ pipenv install flask flask-sqlalchemy gunicorn</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>This will install Flask, Sqlalchemy and gunicorn in our virtual environment.</p><h2 id="Create-our-first-Flask-app"><a href="#Create-our-first-Flask-app" class="headerlink" title="Create our first Flask app"></a><a href="#create-our-first-flask-app"></a>Create our first Flask app</h2><p>Create new file inside <strong>flask_tailwind_todo_app</strong> directory  </p><pre><code>$ touch app.py</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Add  </p><pre><code>from flask import Flaskapp = Flask(__name__)@app.route(&quot;/&quot;)def home():   return &quot;Hello Flask&quot;if __name__ == &quot;__main__&quot;:   app.run(debug=True)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Open your terminal and run  </p><pre><code>$ python app.py</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>You’ll see something like this  </p><pre><code>* Serving Flask app &quot;app&quot; (lazy loading)* Environment: production  WARNING: This is a development server. Do not use it in a production deployment.  Use a production WSGI server instead.* Debug mode: on* Running on http://127.0.0.1:5000/ (Press CTRL+C to quit)* Restarting with stat* Debugger is active!* Debugger PIN: 278-921-051</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Go to <strong><a href="http://127.0.0.1:5000/">http://127.0.0.1:5000/</a></strong> what is the result ?</p><p>If everything work fine you’ll see <strong>Hello Flask</strong><br>Let’s make it great by rendering a template</p><h2 id="Rendering-Templates"><a href="#Rendering-Templates" class="headerlink" title="Rendering Templates"></a><a href="#rendering-templates"></a>Rendering Templates</h2><pre><code>from flask import Flask, render_templateapp = Flask(__name__)@app.route(&quot;/&quot;)def home():   return render_template(&quot;home.html&quot;)if __name__ == &quot;__main__&quot;:   app.run(debug=True)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>If you run reload your browser you’ll see an error like this <strong>jinja2.exceptions.TemplateNotFound</strong></p><p>Let’s fix it by creating our <strong>templates</strong> folder</p><p>Inside <strong>templates</strong> folder create home.html file and add this code.  </p><pre><code>&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;  &lt;head&gt;    &lt;meta charset=&quot;UTF-8&quot; /&gt;    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;    &lt;title&gt;Flask Todo App with Tailwind&lt;/title&gt;  &lt;/head&gt;  &lt;body&gt;    &lt;h1&gt;Hello Flask&lt;/h1&gt;  &lt;/body&gt;&lt;/html&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Reload your browser what you see ?<br>Let’s add some css with tailwind, in this tutorial i will use the <strong>CDN</strong> version.</p><p>Don’t use the <strong>CDN</strong> if you want to have all <strong>Tailwind</strong> features.  </p><pre><code>&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;  &lt;head&gt;    &lt;meta charset=&quot;UTF-8&quot; /&gt;    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;    &lt;link      href=&quot;https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css&quot;      rel=&quot;stylesheet&quot;    /&gt;    &lt;title&gt;Flask Todo App with Tailwind&lt;/title&gt;  &lt;/head&gt;  &lt;body&gt;    &lt;h1 class=&quot;text-center&quot;&gt;Hello Flask&lt;/h1&gt;  &lt;/body&gt;&lt;/html&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>We will use a template from <a href="https://tailwindcomponents.com/component/todo-list-app">Tailwind components</a> here is the source code</p><p>Every website in some point or other will need some custom <strong>css</strong> files, let’s add static files.</p><h2 id="Static-files"><a href="#Static-files" class="headerlink" title="Static files"></a><a href="#static-files"></a>Static files</h2><p>Create <strong>base.html</strong> file and move all the content of <strong>home.html</strong> into it</p><p>base.html  </p><pre><code>&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;  &lt;head&gt;    &lt;meta charset=&quot;UTF-8&quot; /&gt;    &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot; /&gt;    &lt;link      href=&quot;https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css&quot;      rel=&quot;stylesheet&quot;    /&gt;    &lt;link      rel=&quot;stylesheet&quot;      href=&quot;&#123;&#123; url_for('static', filename='css/style.css') &#125;&#125;&quot;    /&gt;    &lt;title&gt;      &#123;% if title %&#125;          &#123;&#123; title &#125;&#125;          &#123;% else %&#125; Flask Todo App with Tailwind          &#123;% endif %&#125;    &lt;/title&gt;  &lt;/head&gt;  &lt;body&gt;    &lt;div      class=&quot;h-100 w-full flex items-center justify-center bg-teal-lightest font-sans&quot;    &gt;      &#123;% block content %&#125;              &#123;% endblock content %&#125;    &lt;/div&gt;  &lt;/body&gt;&lt;/html&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>home.html  </p><pre><code>&#123;% extends "base.html" %&#125; &#123;% block content %&#125;    <div class="bg-white rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg">      <div class="mb-4">        <h1 class="text-grey-darkest">Flask Todo List</h1>        <div class="flex mt-4">          <input            class="shadow appearance-none border rounded w-full py-2 px-3 mr-4 text-grey-darker"            placeholder="Add Todo"          />          <button            class="flex-no-shrink p-2 border-2 rounded text-teal border-teal hover:text-white hover:bg-teal"          >            Add          </button>        </div>      </div>      <div>        <div class="flex mb-4 items-center">          <p class="w-full text-grey-darkest">            Add another component to Tailwind Components          </p>          <button            class="flex-no-shrink p-2 ml-2 border-2 rounded text-red border-red hover:text-white hover:bg-red"          >            Remove          </button>        </div>      </div>    </div>    &#123;% endblock content %&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Everything here is static, let’s add dynamic data…</p><h2 id="Working-with-database-CRUD"><a href="#Working-with-database-CRUD" class="headerlink" title="Working with database (CRUD)"></a><a href="#working-with-database-crud"></a>Working with database (CRUD)</h2><p>We will use <strong>SQlite</strong> as the database adapter  </p><pre><code>from flask import Flask, render_templatefrom flask_sqlalchemy import SQLAlchemy  # addfrom datetime import datetime  # addapp = Flask(__name__)app.config[&#39;SQLALCHEMY_DATABASE_URI&#39;] = &#39;sqlite:///todo.db&#39;  # addapp.config[&#39;SQLALCHEMY_TRACK_MODIFICATIONS&#39;] = False  # adddb = SQLAlchemy(app)  # add# addclass Task(db.Model):   id = db.Column(db.Integer, primary_key=True)   name = db.Column(db.String(80), nullable=False)   created_at = db.Column(db.DateTime, nullable=False,                          default=datetime.now)   def __repr__(self):       return f&#39;Todo : &#123;self.name&#125;&#39;@app.route(&quot;/&quot;)def home():   return render_template(&quot;home.html&quot;)if __name__ == &quot;__main__&quot;:   app.run(debug=True)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Let’s add some data from Python interpreter(<strong>REPL</strong>)  </p><pre><code>$ pythonPython 3.7.5 (default, Nov 20 2019, 09:21:52)[GCC 9.2.1 20191008] on linuxType &quot;help&quot;, &quot;copyright&quot;, &quot;credits&quot; or &quot;license&quot; for more information.&gt;&gt;&gt; from app import db, Task&gt;&gt;&gt; db.create_all()&gt;&gt;&gt; new_task = Task(name=&quot;Learn Flask&quot;)&gt;&gt;&gt; db.session.add(new_task)&gt;&gt;&gt; db.session.commit()&gt;&gt;&gt; tasks = Task.query.all()&gt;&gt;&gt; tasks[Todo : Learn Flask]&gt;&gt;&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><h2 id="Getting-data"><a href="#Getting-data" class="headerlink" title="Getting data"></a><a href="#getting-data"></a>Getting data</h2><p>We added our fist todo successfully from the <strong>REPL</strong> let’s display the in our template <strong>home.html</strong>  </p><pre><code>from flask import Flask, render_templatefrom flask_sqlalchemy import SQLAlchemy  # addfrom datetime import datetime  # addapp = Flask(__name__)app.config[&#39;SQLALCHEMY_DATABASE_URI&#39;] = &#39;sqlite:///todo.db&#39;  # addapp.config[&#39;SQLALCHEMY_TRACK_MODIFICATIONS&#39;] = False  # adddb = SQLAlchemy(app)  # add# addclass Task(db.Model):   id = db.Column(db.Integer, primary_key=True)   name = db.Column(db.String(80), nullable=False)   created_at = db.Column(db.DateTime, nullable=False,                          default=datetime.now)   def __repr__(self):       return f&#39;Todo : &#123;self.name&#125;&#39;@app.route(&quot;/&quot;)def home():   tasks = Task.query.order_by(Task.created_at) # add   return render_template(&quot;home.html&quot;, tasks=tasks) # addif __name__ == &quot;__main__&quot;:   app.run(debug=True)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Change your <strong>home.html</strong> file  </p><pre><code>&#123;% extends "base.html" %&#125;&#123;% block content %&#125;    <div class="bg-white rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg">      <div class="mb-4">        <h1 class="text-grey-darkest">Flask Todo List</h1>        <div class="flex mt-4">          <input            class="shadow appearance-none border rounded w-full py-2 px-3 mr-4 text-grey-darker"            placeholder="Add Todo"          />          <button            class="flex-no-shrink p-2 border-2 rounded text-teal border-teal hover:text-white hover:bg-teal bg-green-600"          >            Add          </button>        </div>      </div>      <div>        &#123;% if tasks %&#125;        &#123;% for task in tasks %&#125;        <div class="flex mb-4 items-center">          <p class="w-full text-grey-darkest">            <a href="#">&#123;&#123; task.name &#125;&#125;</a>          </p>          <button            class="flex-no-shrink p-2 ml-2 border-2 rounded text-red border-red hover:text-white hover:bg-red bg-red-600"          >            Remove          </button>        </div>        &#123;% endfor %&#125;        &#123;% else %&#125;        <p class="text-center">No Task to display</p>        &#123;% endif %&#125;      </div>    </div>    &#123;% endblock content %&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Open your browser, you’ll see a list of tasks.</p><h2 id="Add-new-data"><a href="#Add-new-data" class="headerlink" title="Add new data"></a><a href="#add-new-data"></a>Add new data</h2><p>Let’s create a new task from html templates</p><p>Change the home fonction<br>app.py  </p><pre><code>from flask import Flask, render_template, request, redirect  # addfrom flask_sqlalchemy import SQLAlchemy  # addfrom datetime import datetime  # addapp = Flask(__name__)app.config[&#39;SQLALCHEMY_DATABASE_URI&#39;] = &#39;sqlite:///todo.db&#39;  # addapp.config[&#39;SQLALCHEMY_TRACK_MODIFICATIONS&#39;] = False  # adddb = SQLAlchemy(app)  # add# addclass Task(db.Model):   id = db.Column(db.Integer, primary_key=True)   name = db.Column(db.String(80), nullable=False)   created_at = db.Column(db.DateTime, nullable=False,                          default=datetime.now)   def __repr__(self):       return f&#39;Todo : &#123;self.name&#125;&#39;@app.route(&quot;/&quot;, methods=[&#39;POST&#39;, &#39;GET&#39;])def home():   if request.method == &quot;POST&quot;: # add       name = request.form[&#39;name&#39;]       new_task = Task(name=name)       db.session.add(new_task)       db.session.commit()       return redirect(&#39;/&#39;)   else:       tasks = Task.query.order_by(Task.created_at).all()  # add   return render_template(&quot;home.html&quot;, tasks=tasks)  # addif __name__ == &quot;__main__&quot;:   app.run(debug=True)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>home.html  </p><pre><code>&#123;% extends "base.html" %&#125;&#123;% block content %&#125;    <div class="bg-white rounded shadow p-6 m-4 w-full lg:w-3/4 lg:max-w-lg">      <div class="mb-4">        <h1 class="text-grey-darkest">Flask Todo List</h1>        <div class="flex mt-4">          &#123;% include "partials/_form.html" %&#125;        </div>      </div>      <div>        &#123;% if tasks %&#125;        &#123;% for task in tasks %&#125;        <div class="flex mb-4 items-center">          <p class="w-full text-grey-darkest">            <a href="#">&#123;&#123; task.name &#125;&#125;</a>          </p>          <button            class="flex-no-shrink p-2 ml-2 border-2 rounded text-red border-red hover:text-white hover:bg-red bg-red-600"          >            Remove          </button>        </div>        &#123;% endfor %&#125;        &#123;% else %&#125;            <p class="text-center">No Task to display</p>            &#123;% endif %&#125;      </div>    </div>    &#123;% endblock content %&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Create a new folder inside <strong>templates</strong>, rename it <strong>partials</strong> and put the form into it.</p><p>partials&#x2F;_form.html  </p><pre><code>&lt;form action=&quot;/&quot; method=&quot;post&quot; class=&quot;inline-flex&quot;&gt;  &lt;input    class=&quot;shadow appearance-none border rounded w-full py-2 px-3 mr-4 text-grey-darker&quot;    placeholder=&quot;Add Todo&quot;    type=&quot;text&quot;    name=&quot;name&quot;  /&gt;  &lt;button    class=&quot;flex-no-shrink p-2 border-2 rounded text-teal border-teal hover:text-white hover:bg-teal bg-green-600&quot;    type=&quot;submit&quot;  &gt;    Add  &lt;/button&gt;&lt;/form&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now you can add new task, retrieve all tasks, what about remove a task ?<br>Let’s do it</p><h2 id="Remove-data"><a href="#Remove-data" class="headerlink" title="Remove data"></a><a href="#remove-data"></a>Remove data</h2><p>In app.py  </p><pre><code># remove a task...@app.route(&#39;/delete/&lt;int:id&gt;&#39;)def delete(id):   task = Task.query.get_or_404(id)   try:       db.session.delete(task)       db.session.commit()       return redirect(&#39;/&#39;)   except Exception:       return &quot;There was a problem deleting data.&quot;...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>In home.html  </p><pre><code>...&lt;a  class=&quot;flex-no-shrink p-2 ml-2 border-2 rounded text-red border-red hover:text-white hover:bg-red bg-red-600&quot;  href=&quot;/delete/&#123;&#123; task.id &#125;&#125;&quot;&gt;  Remove&lt;/a&gt;...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>It’s very simple and intuitive, the last part before moving to production is the update task</p><h2 id="Update-data"><a href="#Update-data" class="headerlink" title="Update data"></a><a href="#update-data"></a>Update data</h2><p>In app.py  </p><pre><code>...# update task@app.route(&#39;/update/&lt;int:id&gt;&#39;, methods=[&#39;GET&#39;, &#39;POST&#39;])def update(id):   task = Task.query.get_or_404(id)   if request.method == &#39;POST&#39;:       task.name = request.form[&#39;name&#39;]       try:           db.session.commit()           return redirect(&#39;/&#39;)       except:           return &quot;There was a problem updating data.&quot;   else:       title = &quot;Update Task&quot;       return render_template(&#39;update.html&#39;, title=title, task=task)...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>In update.html  </p><pre><code>&#123;% extends 'base.html' %&#125;&#123;% block content %&#125;    <div class="mb-4">      <h1 class="text-grey-darkest">&#123;&#123; title &#125;&#125;</h1>      <div class="flex mt-4">        <form action="/update/&#123;&#123; task.id &#125;&#125;" method="post" class="inline-flex">          <input            class="shadow appearance-none border rounded w-full py-2 px-3 mr-4 text-grey-darker"            placeholder="Add Todo"            type="text"            name="name"            value="&#123;&#123; task.name &#125;&#125;"          />          <button            class="flex-no-shrink p-2 border-2 rounded text-teal border-teal hover:text-white hover:bg-teal bg-green-600"            type="submit"          >            Update          </button>        </form>      </div>    </div>        &#123;% endblock content %&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now we have fully functional <strong>Flask</strong> app, but if we wanna show the world our new app ?</p><p>Let’s deploy the application using <strong>Heroku</strong></p><h2 id="Deploy-to-Heroku"><a href="#Deploy-to-Heroku" class="headerlink" title="Deploy to Heroku"></a><a href="#deploy-to-heroku"></a>Deploy to Heroku</h2><p>Initialize a new <strong>git</strong> repository  </p><pre><code>$ git init$ touch .gitignore...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>In <strong>.gitignore</strong> file add :  </p><pre><code># Created by https://www.gitignore.io/api/flask# Edit at https://www.gitignore.io/?templates=flask### Flask ###instance/*!instance/.gitignore.webassets-cache### Flask.Python Stack #### Byte-compiled / optimized / DLL files__pycache__/*.py[cod]*$py.class# C extensions*.so# Distribution / packaging.Pythonbuild/develop-eggs/dist/downloads/eggs/.eggs/lib/lib64/parts/sdist/var/wheels/pip-wheel-metadata/share/python-wheels/*.egg-info/.installed.cfg*.eggMANIFEST# PyInstaller#  Usually these files are written by a python script from a template#  before PyInstaller builds the exe, so as to inject date/other infos into it.*.manifest*.spec# Installer logspip-log.txtpip-delete-this-directory.txt# Unit test / coverage reportshtmlcov/.tox/.nox/.coverage.coverage.*.cachenosetests.xmlcoverage.xml*.cover.hypothesis/.pytest_cache/# Translations*.mo*.pot# Scrapy stuff:.scrapy# Sphinx documentationdocs/_build/# PyBuildertarget/# pyenv.python-version# pipenv#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.#   However, in case of collaboration, if having platform-specific dependencies or dependencies#   having no cross-platform support, pipenv may install dependencies that don&#39;t work, or not#   install all needed dependencies.#Pipfile.lock# celery beat schedule filecelerybeat-schedule# SageMath parsed files*.sage.py# Spyder project settings.spyderproject.spyproject# Rope project settings.ropeproject# Mr Developer.mr.developer.cfg.project.pydevproject# mkdocs documentation/site# mypy.mypy_cache/.dmypy.jsondmypy.json# Pyre type checker.pyre/# End of https://www.gitignore.io/api/flask</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Create <strong>Procfile</strong> and add :  </p><pre><code>$ touch Procfile...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><pre><code>web: gunicorn app:app</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Login to <strong>Heroku</strong> with your account  </p><pre><code>$ heroku login...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Commit your code to the repository  </p><pre><code>$ git add .$ git commit -m &quot;Initial commit&quot;...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Create a new app on Heroku  </p><pre><code>heroku create flasktailwindtodo</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Deploy your app to Heroku  </p><pre><code>$ git push heroku master#...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Open your browser from the terminal using Heroku  </p><pre><code>$ heroku open#...</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Congrats 😍 See you in next Tutorial</p>]]></content>
      
      
      
        <tags>
            
            <tag> Python Flask </tag>
            
            <tag> Heroku </tag>
            
            <tag> Tailwind </tag>
            
            <tag> Application </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>React.js - Custom Hooks</title>
      <link href="/2022/12/19/react/"/>
      <url>/2022/12/19/react/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/react.webp" alt="$cover"></p><p>The topic of hooks in React development is quite popular and has been analyzed more than once by various specialists. The purpose of our material is not so much to bring something new as to analyze a sufficiently important and relevant topic in the most accessible and understandable language, so that it does not cause difficulties even for beginners in the development of applications on React.</p><p>First appeared in React version 16.8. The structure of applications based on the use of hooks was very pleasant to the community for its flexibility and simplicity, which allowed it to practically replace classes.</p><h2 id="It’s-easier-with-hooks"><a href="#It’s-easier-with-hooks" class="headerlink" title="It’s easier with hooks!"></a><a href="#its-easier-with-hooks"></a>It’s easier with hooks!</h2><p>Many newcomers were scared away by the classes with their cumbersomeness and less transparency. The mechanisms of state and reusable use looked much more complicated than in reality. The introduction of hooks has largely solved this problem and significantly increased the popularity of the library, which has been steadily growing for several years.</p><h2 id="React’s-main-hooks-out-of-the-box"><a href="#React’s-main-hooks-out-of-the-box" class="headerlink" title="React’s main hooks out of the box"></a><a href="#reacts-main-hooks-out-of-the-box"></a>React’s main hooks out of the box</h2><p>React out of the box contains several very important hooks.</p><h3 id="useState-manage-the-state-in-functional-components"><a href="#useState-manage-the-state-in-functional-components" class="headerlink" title="useState - manage the state in functional components"></a><a href="#usestate-manage-the-state-in-functional-components"></a>useState - manage the state in functional components</h3><p>One of the most important hooks is useState. Its name immediately makes it clear what it is used for - it is responsible for the state. This hook allows you to influence the state like the this.setState() method. First of all, let’s look at a simple example of its use:  </p><pre><code>const MyComponent = () =&gt; &#123;  const [counter, setCounter] = useState(1);  return (    &lt;button onClick=&#123;() =&gt; setCounter(counter + 1)&#125;&gt;      +1 to the previous value    &lt;/button&gt;);&#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>In this example, after the functional element is declared, we call the useState method, passing it the default value:  </p><pre><code>const [counter, setCounter] = useState(1);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>The method returns an array of 2 elements. The 1st element is a state variable, the 2nd element contains a function to change this state. The button is set to the onClick event, which allows you to change the state by increasing the value by 1 from the previous one:  </p><pre><code>onClick=&#123;() =&gt; setCounter(counter + 1)&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><h3 id="useContext-we-transfer-information-to-any-levels-of-nesting"><a href="#useContext-we-transfer-information-to-any-levels-of-nesting" class="headerlink" title="useContext - we transfer information to any levels of nesting"></a><a href="#usecontext-we-transfer-information-to-any-levels-of-nesting"></a>useContext - we transfer information to any levels of nesting</h3><p>As a rule, the parent component shares data with children using props. However, there is a need to transfer this data not only to the nearest “children” of this component, but also to more nested components. Transmitting “relatives” along the entire chain is terribly inconvenient and can lead to mistakes. In this case, it is convenient to use useContext.  </p><pre><code>import &#123;createContext, useContext&#125; from &quot;react&quot;;const MyContext = createContext(&quot;no data&quot;);const Bookcase = () =⟩ &#123;  return (    ⟨MyContext.Provider value=&quot;шкаф #1 &quot;⟩      ⟨Bookshelf /⟩    ⟨/MyContext.Provider⟩  );&#125;;const Bookshelf = () =⟩ &#123;  return ⟨Book /⟩;&#125;;const Book = () =⟩ &#123;  const context = useContext(MyContext);  return `Book in is &quot;$&#123;context&#125;&quot;`;&#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>In this example, thanks to the useContext hook, each book can have information about which cabinet it is located in, and there is no need to receive it from the direct parent (book shelf).</p><h3 id="useEffect-we-implement-lifecycle-functionality"><a href="#useEffect-we-implement-lifecycle-functionality" class="headerlink" title="useEffect - we implement lifecycle functionality"></a><a href="#useeffect-we-implement-lifecycle-functionality"></a>useEffect - we implement lifecycle functionality</h3><p>Used to simulate the lifecycle, as well as the componentDidMount, componentDidUpdate, componentWillUnmount methods do when using the React structure on classes. A callback function is passed as arguments, which performs the necessary actions and an array with variables, the change of which must be monitored and callback when they are changed.</p><h3 id="useRef-link-variables-directly-to-DOM"><a href="#useRef-link-variables-directly-to-DOM" class="headerlink" title="useRef - link variables directly to DOM"></a><a href="#useref-link-variables-directly-to-dom"></a>useRef - link variables directly to DOM</h3><p>To access DOM elements directly in functional components, you must use useRef. In the example below, we bind the button to the buttonRef variable and can use this link in the callback function passed as the 1st argument in useRef.  </p><pre><code>const MyButton = () =⟩ &#123;  const buttonRef = useRef();  useEffect(() =⟩ &#123;    console.log(buttonRef.current.innerHTML);  &#125;, []);  return ⟨button ref=&#123;ref&#125;⟩My button⟨/button⟩;&#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><h2 id="More-hooks"><a href="#More-hooks" class="headerlink" title="More hooks!"></a><a href="#more-hooks"></a>More hooks!</h2><p>useReducer: allows you to store the status value regardless of nesting, is an analogue of Redux.</p><p>useMemo: allows you to store the value and recalculate only when constraints change.</p><p>useCallback: used to memoize functions, avoids unnecessary rendering and re-creation of functions. Very useful for optimization.</p><h2 id="We’re-writing-our-own-hook"><a href="#We’re-writing-our-own-hook" class="headerlink" title="We’re writing our own hook"></a><a href="#were-writing-our-own-hook"></a>We’re writing our own hook</h2><p>In essence, hooks are common functions, the name of which begins with the prefix “use”. They can use other hooks, accept arguments and return the result.</p><p>It is convenient to create your own hooks when you need to learn the logic that is often used in different components of the application. It is necessary to remember about important restrictions for hooks: you can’t call inside conditional constructions and loops - this will cause an error in the application.</p><p>Let’s look at an example from the official react documentation. It is interesting because it describes an example of creating a useReducer hook using an existing useState.</p><p>Let’s say we work with a component that has a fairly developed state management logic depending on their type. In this case, useState is not very convenient to use with centralized state management logic and Redux-reducer is more suitable:  </p><pre><code>function todosReducer(state, action) &#123;  switch (action.type) &#123;    case &#39;add&#39;:      return [...state, &#123;        text: action.text,        completed: false      &#125;];    // ... other actions ...    default:      return state;  &#125;&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Our hook will look like this (simplified):  </p><pre><code>function useReducer(reducer, initialState) &#123;  const [state, setState] = useState(initialState)  function dispatch(action) &#123;    const nextState = reducer(state, action)    setState(nextState)  &#125;  return [state, dispatch]&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Here we accept as parameters - a reworker function responsible for the log of state change, initial state, and return the current state and function to change it. At the same time, we can very easily modify the logic of changing states by adding&#x2F;changing types in the todosReducer function. After that, it is convenient to use this logic in your component:  </p><pre><code>function Todos() &#123;  const [todos, dispatch] = useReducer(todosReducer, [])  function handleAddClick(text) &#123;    dispatch(&#123; type: &#39;add&#39;, text &#125;)  &#125;  // ...&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Hooks can be used for a wide variety of purposes. Let’s look at an example of a hook for working with local browser storage:  </p><pre><code>function getStorageValue(key, defaultValue) &#123;  const saved = localStorage.getItem(key);  const initial = !saved || saved === &#39;undefined&#39; ? null : JSON.parse(saved);  return initial || defaultValue;&#125;export default  function useLocalStorage (key, defaultValue)&#123;  const [value, setValue] = useState(() =⟩ &#123;    return getStorageValue(key, defaultValue);  &#125;);  useEffect(() =⟩ &#123;    localStorage.setItem(key, JSON.stringify(value));  &#125;, [key, value]);  return [value, setValue];&#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>We defined the function of working with localStorage by writing our own logic for obtaining primary data from the repository. Then we created a hook that initializes the state using the result of this function. We also use useEffect to write to localStorage when changing the value of the key or value variables. In our component, we can use the hook in this way:  </p><pre><code>const [storageData, setStorageData] = useLocalStorage(&#39;my-data&#39;);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>So, we managed to link data in the state with the data in the local browser storage.</p><p>That’s all for now, I hope this article helped make understanding the topic of hooks easier for you!</p><p>Thanks for reading. Leave your comments below and follow my <a href="https://github.com/Phantasm0009">GitHub</a> &lt;3</p>]]></content>
      
      
      
        <tags>
            
            <tag> React </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>20 Git Commands You (Probably) Didn&#39;t Know About 🧙‍♂️</title>
      <link href="/2022/12/18/git/"/>
      <url>/2022/12/18/git/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/git.webp" alt="$cover"></p><p>If you’ve ever browsed the <a href="https://git-scm.com/docs">git manual</a> (or run <code>man git</code>), then you’ll have noticed there’s a whole lot more to git than what most of us use on a daily basis. A lot of these commands are incredibly powerful and can make your life a lot easier (others are a bit niche, but still good to know).</p><blockquote><p>This post outlines 20 of my favourite under-used git features, which you can use to level up your development process, impress your colleagues, help you answer git interview questions, and most importantly - have fun with!</p></blockquote><h3 id="Contents-and-TL-DR"><a href="#Contents-and-TL-DR" class="headerlink" title="Contents (and TL;DR)"></a><a href="#contents-and-tldr"></a>Contents (and TL;DR)</h3><ul><li><a href="#git-web">Git Web</a> - <em>Open builtin GUI</em></li><li><a href="#git-notes">Git Notes</a> - <em>Attach extra info to commits</em></li><li><a href="#git-bisect">Git Bisect</a> - <em>Debug like a pro</em></li><li><a href="#git-grep">Git Grep</a> - <em>Search for anything</em></li><li><a href="#git-archive">Git Archive</a> - <em>Compress project for sharing</em></li><li><a href="#git-submodules">Git Submodules</a> - <em>Import other repos into yours</em></li><li><a href="#git-bugreport">Git Bugreport</a> - <em>Compile bug report with system info</em></li><li><a href="#git-fsck">Git Fsck</a> - <em>Verify and recover unreachable objects</em></li><li><a href="#git-stripspace">Git Stripspace</a> - <em>Remove trailing whitespaces</em></li><li><a href="#git-diff">Git Diff</a> - <em>Compare changes between two files</em></li><li><a href="#git-hooks">Git Hooks</a> - <em>Execute script when a git command is run</em></li><li><a href="#git-blame">Git Blame</a> - <em>Show who wrote a given line</em></li><li><a href="#git-lfs">Git Large File Storage</a> - <em>Store big files in git</em></li><li><a href="#git-gc">Git Garbage Collection</a> - <em>Optimize your git repo</em></li><li><a href="#git-show">Git Show</a> - <em>Easily inspect any git object</em></li><li><a href="#git-describe">Git Describe</a> - <em>Give readable name based on last tag</em></li><li><a href="#git-tag">Git Tag</a> - <em>Create version tags at specific points</em></li><li><a href="#git-reflog">Git Reflog</a> - <em>List all git actions made on a repo</em></li><li><a href="#git-log">Git Log</a> - <em>View commit log, and branch diagrams</em></li><li><a href="#git-cherry-pick">Git Cherry Pick</a> - <em>Pull a feature into your branch</em></li><li><a href="#git-switch">Git Switch</a> - <em>Quickly jump between branches</em></li><li><a href="#bonus">Bonus</a> - <em>Extend git with more commands!</em></li></ul><hr><h2 id="Git-Web"><a href="#Git-Web" class="headerlink" title="Git Web"></a><a href="#git-web"></a>Git Web</h2><blockquote><p>Run <a href="https://git-scm.com/docs/git-instaweb"><code>git instaweb</code></a> to instantly browse your working repository in gitweb</p></blockquote><p>Git has a built-in <a href="https://git-scm.com/docs/gitweb">web-based visualiser</a> for browsing local repositories, which lets you view and manage your repo via a GUI in the browser. It’s packed with useful features, including:</p><ul><li>Browsing and stepping through revisions and inspecting diffs, file contents and metadata</li><li>Viewing commit logs, branches, directories, file history, and attached data visually</li><li>Generating RSS or Atom feeds of commits and repository activity logs</li><li>Searching commits, files, changes and diffs</li></ul><p>To open it, just run <code>git instaweb</code> from within your repo. Your browser should pop open, and load <code>http://localhost:1234</code>. If you don’t have Lighttpd installed, you can specify an alternative web server with the <code>-d</code> flag. Other options can be configured either via flags (like <code>-p</code> for port, <code>-b</code> for the browser to open, etc), or under the <code>[instaweb]</code> block in your git config.</p><p>There’s also the <code>git gui</code> command, which can open up a GUI-based git app</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--ATnCqGFm--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://i.ibb.co/0DrmcWG/Screenshot-from-2022-12-17-20-26-30.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--ATnCqGFm--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://i.ibb.co/0DrmcWG/Screenshot-from-2022-12-17-20-26-30.png" alt="Screenshot of Git GUI"></a></p><hr><h2 id="Git-Notes"><a href="#Git-Notes" class="headerlink" title="Git Notes"></a><a href="#git-notes"></a>Git Notes</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-notes"><code>git notes</code></a> to add extra info to commits</p></blockquote><p>Sometimes you need to attach additional data to a git commit (beyond just the changes, message, date-time and author info).</p><p>The notes are stored within <code>.git/refs/notes</code>, and since that’s separate from the commit object data, you can modify the notes associated with a commit at anytime, without the SHA-1 hash changing.</p><p>You can view notes with <code>git log</code>, using most git GUI apps, or with the <code>git notes show</code> command. Some git hosts also show notes in the commit view (although <a href="https://github.blog/2010-08-25-git-notes-display/">GH no longer displays notes</a>).</p><hr><h2 id="Git-Bisect"><a href="#Git-Bisect" class="headerlink" title="Git Bisect"></a><a href="#git-bisect"></a>Git Bisect</h2><blockquote><p>With <a href="https://git-scm.com/docs/git-bisect"><code>git bisect</code></a> you can find the commit that introduced a bug using binary search</p></blockquote><p>This is one of the most powerful, yet easy to use git commands- bisect is an absolute life saver when it comes to debugging. After starting a bisect, it checks out commits for you, and you tell it weather that commit is <code>good</code> (no bug), or <code>bad</code> (bug introduced), which lets you narrow down the the earliest commit where the bug is present.</p><p>To get started, run <code>git bisect start</code> then pass it a known good commit with <code>git bisect good &lt;commit-hash&gt;</code> and a known bad one (defaults to current) with <code>git bisect bad &lt;optional-hash&gt;</code>. It will then checkout the commit in-between the good and bad commits, then you specify weather the bug is present with either <code>git bisect good</code> or <code>git bisect bad</code>. It will then repeat the process, checking out a commit in the center of the bad and good, all the way until you’ve found the exact commit that introduced the bug. Cancel anytime with <code>git bisect reset</code>.</p><p>There’s much more to the bisect command, including replays, viewing commits, skipping, so it’s worth checking out the docs next time your debugging.</p><hr><h2 id="Git-Grep"><a href="#Git-Grep" class="headerlink" title="Git Grep"></a><a href="#git-grep"></a>Git Grep</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-grep"><code>git grep</code></a> to search for code, files, commits or anything else across your repo</p></blockquote><p>Ever find yourself needing to search for a string anywhere within a git project? With git grep you can easily search for any string, or RegEx throughout your project, and across branches (like a more powerful Ctrl + F!).</p><p><code>git grep &lt;regexp&gt; &lt;ref&gt;</code></p><p>It includes plenty of <a href="https://git-scm.com/docs/git-grep#_options">options</a> to narrow down your search, or specify results format. For example, use <code>-l</code> to only return file names, <code>-c</code> to specify number of matches per file to return, <code>-e</code> to exclude results matching a condition, <code>--and</code> to specify multiple conditions, <code>-n</code> to search with line number.</p><p>Since git grep is regex-compatible, you can get much more advanced with the string your searching for.<br>You can also use it to specify a file extension, like <code>git grep &#39;console.log&#39; *.js</code> which will show all console.logs from within JavaScript files</p><p>The second parameter is a ref, and can be a branch name, commit, range of commits, or anything else. E.g. <code>git grep &quot;foo&quot; HEAD~1</code> will search the previous commit.</p><hr><h2 id="Git-Archive"><a href="#Git-Archive" class="headerlink" title="Git Archive"></a><a href="#git-archive"></a>Git Archive</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-archive"><code>git archive</code></a> to combine an entire repo into a single file</p></blockquote><p>When sharing or backing up a repository, it’s often preferred to store it as a single file. Using git archive will include all repo history, so it can be easily extracted back to it’s original form. The command also includes a lot of additional options, so you can customise exactly what files are and aren’t included in the archive.  </p><pre><code>git archive --format=tar --output=./my-archive HEAD</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><hr><h2 id="Git-Submodules"><a href="#Git-Submodules" class="headerlink" title="Git Submodules"></a><a href="#git-submodules"></a>Git Submodules</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-submodule"><code>git submodule</code></a> to pull any other repository into yours</p></blockquote><p>In git, <a href="https://git-scm.com/docs/gitsubmodules">submodules</a> let you mount one repo into another, and is commonly used for core dependencies or splitting components into separate repositories. For more info, see <a href="https://notes.aliciasykes.com/17996/quick-tip-git-submodules">this post</a>.</p><p>Running the following command will pull a module into the specified location, and also create a <code>.gitmodules</code> file so that it’s always downloaded when the repo is cloned. Use the <code>--recursive</code> flag to include sub-modules when cloning the repo.  </p><pre><code>git submodule add https://github.com/&lt;user&gt;/&lt;repo&gt; &lt;path/to/save/at&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>There’s also <a href="https://www.atlassian.com/git/tutorials/git-subtree"><code>git subtree</code></a>, which does a similar thing, but without the need for metadata files.</p><hr><h2 id="Git-Bug-Report"><a href="#Git-Bug-Report" class="headerlink" title="Git Bug Report"></a><a href="#git-bug-report"></a>Git Bug Report</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-bugreport"><code>git bugreport</code></a> to compose a bug ticket, including git and system info</p></blockquote><p>This command will capture system info, and then open up a standard bug template (steps to reproduce, actual + expected output, etc). The completed file should be a very complete bug report, with all necessary info captured.</p><p>This is very handy if your a maintainer for an open source package and asking a user (developer) to raise a bug report, as it ensures all necessary data is included.</p><p>And if you are raising a bug report to the core git system, you can also run the <a href="https://git-scm.com/docs/git-diagnose"><code>git diagnose</code></a> command, and then raise your issue <a href="https://github.com/git/git">here</a>.</p><hr><h2 id="Git-Fsck"><a href="#Git-Fsck" class="headerlink" title="Git Fsck"></a><a href="#git-fsck"></a>Git Fsck</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-fsck"><code>git fsck</code></a> to check all objects, or recover unreachable ones</p></blockquote><p>Although not often needed, sometimes you may have to verify the objects stored by git. This is where fsck (or File System ChecK) comes in, it tests the object database and verifies the SHA-1 ID of all objects and the connections they make.</p><p>It can also be used alongside the <code>--unreachable</code> flag to find objects that are no longer reachable from any named reference (since unlike other commands, it includes everything in <code>.git/objects</code>).</p><hr><h2 id="Git-Stripspace"><a href="#Git-Stripspace" class="headerlink" title="Git Stripspace"></a><a href="#git-stripspace"></a>Git Stripspace</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-stripspace"><code>git stripspace</code></a> to format whitespaces within a given file</p></blockquote><p>Best practice is to avoid trailing whitespaces at the end of lines, avoid having multiple consecutive blank lines, avoid empty lines from beginning and end of an input, and end each file with a new line. There’s plenty of language-specific tools which do this automatically for you (like prettier), but Git also has this functionality builtin.</p><p>It’s intended for metadata (commit messages, tags, branch descriptions, etc), but also works if you pipe a file to it, then pipe the response back to a file. E.g. <code>cat ./path-to-file.txt | git stripspace</code> or <code>git stripspace &lt; dirty-file.txt &gt; clean-file.txt</code></p><p>You can also use this to remove comments (with <code>--strip-comments</code>), or even comment out lines (with <code>--comment-lines</code>).</p><hr><h2 id="Git-Diff"><a href="#Git-Diff" class="headerlink" title="Git Diff"></a><a href="#git-diff"></a>Git Diff</h2><blockquote><p>With <a href="https://git-scm.com/docs/git-diff"><code>git diff</code></a> you can compare the difference between 2 sets of code</p></blockquote><p>You’re probably aware that you you can run <code>git diff</code> to show all changes since the last commit, or use <code>git diff &lt;commit-sha&gt;</code> to compare either 2 commits, or 1 commit to the HEAD. But there’s much more you can do with the diff command.</p><p>You can also use it to compare any two arbitrary files, with <code>diff file-1.txt file-2.txt</code> (no more visiting <a href="https://www.diffchecker.com/compare/">diffchecker.com</a>!)</p><p>Or compare 2 branches, or refs with each other, using <code>git diff branch1..branch2</code></p><p>Note that a double dot (<code>..</code>) is the same as a space and indicates the diff input should be the tip of the branches, but you can also use a triple dot (<code>...</code>) to convert the first parameter into a ref of the shared common ancestor commit between the two diff inputs - very useful! If you want to only compare a single file across branches, just pass the files name in as the third argument.</p><p>You may want to see all changes made within a given date range, for this use <code>git diff HEAD@&#123;7.day.ago&#125; HEAD@&#123;0&#125;</code> (for the last week), again this can be paired up with a filename, branch names, specific commits or any other ref.</p><p>There’s also the <a href="https://www.git-scm.com/docs/git-range-diff"><code>git range-diff</code></a> command, which provides a simple interface for comparing commit ranges.</p><p>There’s much more to the git diff tool (as well as the option of using your own diff checker), so I recommend checking out <a href="https://git-scm.com/docs/git-diff#_description">the docs</a>.</p><hr><h2 id="Git-Hooks"><a href="#Git-Hooks" class="headerlink" title="Git Hooks"></a><a href="#git-hooks"></a>Git Hooks</h2><blockquote><p>Use <a href="https://git-scm.com/docs/githooks"><code>hooks</code></a> to execute command or run scripts when a given get action occurs</p></blockquote><p>Hooks let you automate pretty much anything. For example: ensuring standards are met (commit message, branch name, patch size), code quality (tests, lint), appending additional info to a commit (user, device, ticket ID), calling a webhook to record an event or run a pipeline, etc.</p><p>There’s pre and post <a href="https://git-scm.com/docs/githooks">hooks available</a> for most git events, like commit, rebase, merge, push, update, applypatch, etc.</p><p>Hooks are stored in <code>.git/hooks</code> (unless you configure them elsewhere with <code>git config core.hooksPath</code>), and can be tested with the <a href="https://git-scm.com/docs/git-hook"><code>git hook</code></a> command. Since they’re just shell files, they can be used to run any command.</p><p>Hooks aren’t pushed to the remote repository, so to share and manage them across your team, you’ll need to use a <a href="https://github.com/aitemr/awesome-git-hooks#tools">hook manager</a>, like <a href="https://github.com/evilmartians/lefthook">lefthook</a> or <a href="https://github.com/typicode/husky">husky</a>. There’s also several <a href="https://githooks.com/#projects">3rd-party tools</a>, which make managing hooks easier, I recommend <a href="https://github.com/sds/overcommit">overcommit</a>.</p><p>Remember, hooks can always be skipped (with the <code>--no-verify</code> flag), so never rely purely on hooks, especially for anything security related.</p><hr><h2 id="Git-Blame"><a href="#Git-Blame" class="headerlink" title="Git Blame"></a><a href="#git-blame"></a>Git Blame</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-blame"><code>git blame</code></a> to show author info for a specific revision and line</p></blockquote><p>A classic, quickly find out who wrote a specific line of code (aka which of your co-workers to blame for the bug!). But it’s also useful to determine at which point in time something changed and inspect that commit and associated metadata.</p><p>For example, to view author and commit info for line 400 to 420 of index.rs, you’d run:  </p><pre><code>git blame -L 400,420 index.rs</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><hr><h2 id="Git-LFS"><a href="#Git-LFS" class="headerlink" title="Git LFS"></a><a href="#git-lfs"></a>Git LFS</h2><blockquote><p>Store large files using <a href="https://git-lfs.github.com/"><code>git lfs</code></a> to not bog down your repo</p></blockquote><p>Often your project will contain larger files (such as databases, binary assets, archives or media files), which would slow down the git workflow and max out usage limits. That’s where <a href="https://git-lfs.github.com/">Large File Storage</a> comes in - it enables you to store these large assets elsewhere, while keeping them trackable with git and maintaining the same access controls&#x2F; permissions. LFS works by replacing these larger files with text pointers that are tracked within git.</p><p>To use it, just run <code>git lfs track &lt;file glob&gt;</code>, which will update your <code>.gitattributes</code> file. You can specify files by their extension (e.g. <code>*.psd</code>), directory, or individually. Running <code>git lfs ls-files</code> to view a list of tracked LFS files.</p><hr><h2 id="Git-GC"><a href="#Git-GC" class="headerlink" title="Git GC"></a><a href="#git-gc"></a>Git GC</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-gc"><code>git gc</code></a> to optimize your repository</p></blockquote><p>Over time git repos accumulate various types of garbage, which take up disk space, and slow down actions. That’s where the built-in garbage collector comes in. Running <code>git gc</code> will remove orphaned and inaccessible commits (with <a href="https://git-scm.com/docs/git-prune"><code>git prune</code></a>), compress file revisions and stored git objects, as well as some other general house keeping tasks like packing refs, pruning reflog, revere metadata or stale working trees and updating indexes.</p><p>Adding the <code>--aggressive</code> flag will <a href="https://git-scm.com/docs/git-gc#_aggressive">aggressively optimize</a> the repository, throwing away any existing deltas and re-computing them, this takes much longer to run but may be needed if you’ve got a large repository.</p><hr><h2 id="Git-Show"><a href="#Git-Show" class="headerlink" title="Git Show"></a><a href="#git-show"></a>Git Show</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-show"><code>git show</code></a> to easily inspect any git object</p></blockquote><p>Outputs objects (blobs, treees, tags or commits) in an easily readable form. To use, just run <code>git show &lt;object&gt;</code>. You’ll likely also want to append the <code>--pretty</code> flag, for a clearer output, but there’s many other options available to customize the output (with <code>--format</code>), so this command can be extremely powerful for displaying exactly what you need.</p><p>An instance that this is very useful for, is previewing a file in another branch, without switching branches. Just run <code>git show branch:file</code></p><hr><h2 id="Git-Describe"><a href="#Git-Describe" class="headerlink" title="Git Describe"></a><a href="#git-describe"></a>Git Describe</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-describe"><code>git describe</code></a> to find the latest tag reachable from a commit, and give it a human-readable name</p></blockquote><p>Run <code>git describe</code> and you’ll see a human-readable string made from combining the last tag name, with the current commit, to generate a string. You can also pass a specific tag to it,</p><p>Note that you must have created tags for this to work, unless you append the <code>--all</code> flag. Git describe will also only use annotated tags by default, so you must specify the <code>--tags</code> flag to make it use lightweight tags as well.</p><hr><h2 id="Git-Tag"><a href="#Git-Tag" class="headerlink" title="Git Tag"></a><a href="#git-tag"></a>Git Tag</h2><blockquote><p>Tag a specific point in your repo’s history using <a href="https://git-scm.com/docs/git-tag"><code>git tag</code></a></p></blockquote><p>It’s often useful to be able to <a href="https://git-scm.com/book/en/v2/Git-Basics-Tagging">tag</a> specific, important points in a repository’s history most commonly used to denote versions of releases. Creating a tag is as simple as <code>git tag &lt;tagname&gt;</code>, or you can tag a historical commit with <code>git tag -a v4.2.0 &lt;commit sha&gt;</code>. Like with commits, you can include a message alongside a tag, using <code>-m</code>.</p><p>Don’t forget to push your tag to remote, with <code>git push origin &lt;tagname&gt;</code>.<br>To list all tags, just run <code>git tag</code>, and optionally use <code>-l</code> for a wildcard search.<br>You’ll then be able to checkout a specific tag, with <code>git checkout &lt;tagname&gt;</code></p><hr><h2 id="Git-Reflog"><a href="#Git-Reflog" class="headerlink" title="Git Reflog"></a><a href="#git-reflog"></a>Git Reflog</h2><blockquote><p>List all updates made on your repo using <a href="https://git-scm.com/docs/git-reflog"><code>git reflog</code></a></p></blockquote><p>Git keeps track of updates to the tip of branches using a mechanism called reference logs, or “reflogs”. Various events are tracked, including: clone, pull, push, commit, checkout and merge. It’s often useful to be able to find an events reference, as many commands accept a ref as a parameter. Just run <code>git reflog</code> to view recent events on <code>HEAD</code>.</p><p>One thing that reflog is really useful for is recovering lost commits. Git never really loses anything, even when rewriting history (like rebasing or commit amending). Reflog allows you to go back to commits even though they are not referenced by any branch or tag.</p><p>By default reflog uses <code>HEAD</code> (your current branch), but you can run reflog on any ref. For example <code>git reflog show &lt;branch name&gt;</code>, or to see stashed changes with <code>git reflog stash</code>. Or show all references with <code>git reflog show --all</code></p><hr><h2 id="Git-Log"><a href="#Git-Log" class="headerlink" title="Git Log"></a><a href="#git-log"></a>Git Log</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-log"><code>git log</code></a> to view a list of commits</p></blockquote><p>You’re likely already familiar with running <code>git log</code> to view a list of recent commits on your current branch. But there’s a few things more you can do with git log.</p><p>Using <code>git log --graph --decorate --oneline</code> will show a nice neat commit graph along with ref pointers.</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--EtfqYNeJ--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://i.ibb.co/c1WByg8/Screenshot-from-2022-12-17-20-43-56.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--EtfqYNeJ--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://i.ibb.co/c1WByg8/Screenshot-from-2022-12-17-20-43-56.png" alt="example git log output"></a></p><p>You also often need to be able to filter logs based on various parameters, the most useful of which are:</p><ul><li><code>git log --search=&quot;&lt;anything&gt;&quot;</code> - Search logs for specific code changes</li><li><code>git log --author=&quot;&lt;pattern&gt;&quot;</code> - Show log only for specific author(s)</li><li><code>git log --grep=&quot;&lt;pattern&gt;&quot;</code> - Filter log using search term or regex</li><li><code>git log &lt;since&gt;..&lt;until&gt;</code> - Show all commits between two references</li><li><code>git log -- &lt;file&gt;</code> - Show all commits made only to a specific file</li></ul><p>Or, just run <code>git shortlog</code> for a summerized list of commits.</p><hr><h2 id="Git-Cherry-Pick"><a href="#Git-Cherry-Pick" class="headerlink" title="Git Cherry Pick"></a><a href="#git-cherry-pick"></a>Git Cherry Pick</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-cherry-pick"><code>git cherry-pick</code></a> to pick specified commit(s) by reference and append them to the working HEAD</p></blockquote><p>Sometimes you need to pull a specific commit from elsewhere, into your current branch. This can be very useful for applying hot fixes, undoing changes, restoring lost commits and in certain team collaboration settings. Note that often traditional merges are better practice, since cherry picking commits can cause duplicate commits in the log.</p><p>Usage is straightforward, just run <code>git cherry-pick &lt;commit-hash&gt;</code>. This will pull the specified commit into your current branch.</p><hr><h2 id="Git-Switch"><a href="#Git-Switch" class="headerlink" title="Git Switch"></a><a href="#git-switch"></a>Git Switch</h2><blockquote><p>Use <a href="https://git-scm.com/docs/git-switch"><code>git switch</code></a></p></blockquote><p>Moving between branches is something that we do often, the <code>switch</code> command is like a simplified version of <code>git checkout</code>, it can be used to create and navigate between branches, but unlike checkout does not copy modified files when you move between branches.</p><p>Similar to <code>checkout -b</code>, with the switch command you can append the <code>-c</code> flag to create a new branch, and jump strait into it, e.g. <code>git switch -c &lt;new branch&gt;</code>. And running <code>git switch -</code> will discard any experimental changes you’ve made, and return you to your previous branch.</p><hr><h2 id="Git-Standup"><a href="#Git-Standup" class="headerlink" title="Git Standup"></a><a href="#git-standup"></a>Git Standup</h2><blockquote><p>Use <a href="https://github.com/kamranahmedse/git-standup"><code>git standup</code></a> to recall what you did on the last working day , based on git commits</p></blockquote><p>I’ve put this one at the end, as it’s not included with most git clients, but you can <a href="https://github.com/kamranahmedse/git-standup#install">easily install it</a> either with your systems package manager, using a 1-line curl script, or by building from source.</p><p>If your boss requires you do a daily standup, to give updates on yesterdays work, but you can never remember what you actually did - this one is for you! It’ll show a nicely formatted list of everything done in a given time frame. Usage is simple, just run <code>git standup</code>, or use <a href="https://github.com/kamranahmedse/git-standup#options">these options</a> to specify what data should be shown (author, timeframe, branches, etc.</p><hr><h2 id="Bonus"><a href="#Bonus" class="headerlink" title="Bonus"></a><a href="#bonus"></a>Bonus</h2><p>Git can be easily extended with add-ons, to add extra commands that do useful tasks. One of the most complete extensions is <a href="https://github.com/tj/git-extras/blob/master/Commands.md">git-extras</a> by <a href="https://github.com/tj">@tj</a>. It gives you 70+ <a href="https://github.com/tj/git-extras/blob/master/Commands.md">extra commands</a> to automate common git jobs.</p><p>For a list of more useful git add-ons, see <a href="https://github.com/stevemao/awesome-git-addons">stevemao&#x2F;awesome-git-addons</a>.</p><p>If you’re working with GitHub repos, then the <a href="https://cli.github.com/">GitHub CLI</a> let’s you do common tasks (managing PRs, issues, code reviews, etc) from the command line.</p><hr><p>If you like this kind of stuff,<br>consider following for more :)</p><p><a href="https://github.com/Lissy93"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--8_HGggCT--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://img.shields.io/badge/-Lissy93-3a3a3a%3Fstyle%3Dflat%26logo%3DGitHub%26logoColor%3Dwhite" alt="Follow Lissy93 on GitHub"></a><a href="https://twitter.com/Lissy_Sykes"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--BhCWBIgy--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://img.shields.io/badge/-%40Lissy_Sykes-00acee%3Fstyle%3Dflat%26logo%3DTwitter%26logoColor%3Dwhite" alt="Follow Lissy_Sykes on Twitter"></a></p>]]></content>
      
      
      
        <tags>
            
            <tag> Git </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>5 Reasons Why Every React Developer Should Learn TypeScript</title>
      <link href="/2022/12/17/typescript/"/>
      <url>/2022/12/17/typescript/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/type.webp" alt="$cover"></p><p>As a React developer, you’re always looking for ways to improve the quality and maintainability of your code. That’s where learning TypeScript comes in! Here are five reasons why every React developer should consider adding TypeScript to their toolkit:</p><h2 id="1-TypeScript-can-catch-errors-before-you-even-run-your-code"><a href="#1-TypeScript-can-catch-errors-before-you-even-run-your-code" class="headerlink" title="1. TypeScript can catch errors before you even run your code"></a><a href="#1-typescript-can-catch-errors-before-you-even-run-your-code"></a>1. TypeScript can catch errors before you even run your code</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--VWC9quyV--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ynfsq6z8bfdpifjrx1po.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--VWC9quyV--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ynfsq6z8bfdpifjrx1po.png" alt="TypeScript can catch errors before you even run your code"></a></p><p>TypeScript’s strong type system allows you to specify the expected data types of variables, function arguments, and return values. This can catch type-related errors early on in the development process, saving you time and frustration debugging issues in production.</p><h2 id="2-TypeScript-can-make-your-code-self-explanatory"><a href="#2-TypeScript-can-make-your-code-self-explanatory" class="headerlink" title="2. TypeScript can make your code self-explanatory"></a><a href="#2-typescript-can-make-your-code-selfexplanatory"></a>2. TypeScript can make your code self-explanatory</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--O3yMfSS2--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/pao7b4iquxjfd6p5kmwi.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--O3yMfSS2--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/pao7b4iquxjfd6p5kmwi.png" alt="TypeScript can make your code self-explanatory"></a></p><p>By explicitly defining types, your code becomes easier to understand for yourself and other developers. This can make it easier to maintain and modify your codebase over time.</p><h2 id="3-TypeScript-is-becoming-more-popular-among-developers-and-companies"><a href="#3-TypeScript-is-becoming-more-popular-among-developers-and-companies" class="headerlink" title="3. TypeScript is becoming more popular among developers and companies"></a><a href="#3-typescript-is-becoming-more-popular-among-developers-and-companies"></a>3. TypeScript is becoming more popular among developers and companies</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--XGgUZs5D--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/tbojrx0yzuqwghkat343.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--XGgUZs5D--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/tbojrx0yzuqwghkat343.png" alt="TypeScript is becoming more popular among developers and companies"></a><br><em>Source: <a href="https://css-tricks.com/the-relevance-of-typescript-in-2022/">https://css-tricks.com/the-relevance-of-typescript-in-2022/</a></em></p><p>In recent years, TypeScript has gained widespread adoption among developers and companies. As a result, many employers are now seeking candidates with TypeScript experience. Adding TypeScript to your skillset can make you a more attractive job candidate and open up new opportunities for your career.</p><h2 id="4-TypeScript-can-improve-the-reliability-of-your-tests"><a href="#4-TypeScript-can-improve-the-reliability-of-your-tests" class="headerlink" title="4. TypeScript can improve the reliability of your tests"></a><a href="#4-typescript-can-improve-the-reliability-of-your-tests"></a>4. TypeScript can improve the reliability of your tests</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--dUdeubWI--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/lw1fmcbf31zzgqbg7ih2.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--dUdeubWI--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/lw1fmcbf31zzgqbg7ih2.png" alt="TypeScript can improve the reliability of your tests"></a></p><p>By specifying the expected inputs and outputs of your functions with TypeScript’s type system, you can write more reliable tests for your code. This can give you confidence that your code is working as intended and catch any potential issues before they make it to production.</p><h2 id="5-TypeScript-can-make-refactoring-your-code-easier"><a href="#5-TypeScript-can-make-refactoring-your-code-easier" class="headerlink" title="5. TypeScript can make refactoring your code easier"></a><a href="#5-typescript-can-make-refactoring-your-code-easier"></a>5. TypeScript can make refactoring your code easier</h2><p>GIF</p><p><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--KW4uOkLK--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_66%2Cw_880/https://user-images.githubusercontent.com/972891/39023162-e2d99498-43ed-11e8-93a2-14287765aad8.gif" alt="TypeScript can make refactoring your code easier"></p><p>Having clear type definitions can make it easier to refactor your code with confidence, knowing that you won’t accidentally break any important type relationships. This can save you time and effort when making changes to your codebase.</p><p>In conclusion, learning TypeScript as a React developer can bring numerous benefits to your projects and career. Its strong type system and seamless integration with React can improve the quality and maintainability of your code, making you a more valuable and sought-after developer.</p><p>To fully take advantage of the benefits that TypeScript offers as a React developer, here are a few tips to keep in mind:</p><ul><li>Gradually introduce TypeScript into your workflow by adding it to one or two projects at a time. This will allow you to get a feel for how it works and how it can improve your code.</li><li>Make use of TypeScript’s interface and class types to create custom types and reusable components.</li><li>Don’t be afraid to ask for help or consult the TypeScript documentation as you learn. The TypeScript community is very active and helpful, and there are many resources available online to assist you.</li><li>Keep an open mind and be willing to experiment with different approaches. As with any new tool, it may take some time to find what works best for you and your projects.</li></ul><p>By following these tips and consistently using TypeScript in your React projects, you’ll be well on your way to reaping the benefits of this powerful tool. Happy coding!</p>]]></content>
      
      
      
        <tags>
            
            <tag> typescript </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>How to Create a Google Maps Clone Using OpenStreetMap API</title>
      <link href="/2022/12/17/maps/"/>
      <url>/2022/12/17/maps/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/maps.webp" alt="$cover"></p><p>Recently, I have been interested in making some projects such as a MERN stack Zillow clone, or a Django travel booking site. However, in researching the tools needed to create both of these, one critical piece was always too expensive: the google maps api. This API is billed per request, so if you are attaching it to a client application that end users can interact with, this will potentially drain your bank account (without proper captcha &amp; security). The solution to this, instead, is to use a free and open source location data API. You can also follow along with this guide with the full Github <a href="https://github.com/brandonbyr4/maps-clone">source code</a>.</p><p><strong>What we are building</strong><br>Instead of relying on a paid service, this project uses completely free tools. It is built with the following.</p><ul><li>Node.js</li><li>Express.js (server)</li><li>EJS (JavaScript templates)</li><li>OpenStreetMap (Location data API)</li><li>Leaflet.js (Interactive map widget)</li></ul><p><strong>Getting started</strong><br>To start, create a new folder, and open a command line in the directory. From there, initialize a new npm &#x2F; node.js project.  </p><pre><code>npm init</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now, install the required dependencies. We are using express for the web server and EJS (JavaScript HTML templates) for the frontend. Also install nodemon so we don’t have to refresh the server after every change.  </p><pre><code>npm install express ejsnpm install --save-dev nodemon</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Lastly, open package.json and replace “scripts” with the following.  </p><pre><code>&quot;scripts&quot;: &#123;    &quot;dev&quot;: &quot;nodemon server.js&quot;,    &quot;start&quot;: &quot;node server.js&quot; &#125;,</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p><strong>Building the server</strong><br>Now, we are ready to build the express.js server to render our site. Copy and paste the following into a new file in the root of your project named “server.js”.  </p><pre><code>const express = require(&quot;express&quot;);const app = express()app.set(&quot;view engine&quot;, &quot;ejs&quot;)app.use(express.urlencoded(&#123; extended: false &#125;))app.use(express.static(__dirname + &#39;/static&#39;));app.get(&quot;/&quot;, (req, res) =&gt; &#123;    res.render(&#39;index&#39;)&#125;)app.listen(process.env.PORT || 8000)</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>This gives us a basic express server that renders our index page. It is also setup to load CSS, images, and JavaScript from a folder named “static” in the root as well.</p><p><strong>Adding the frontend</strong><br>Our web server should be running, however, there is no index file to render yet. Create a file named “index.ejs” inside a new folder named “views”. Then, type “!” and hit enter (if your using VS code) to create a new HTML file. Paste the following into the body of your HTML file.  </p><pre><code>&lt;div class=&quot;map-container&quot;&gt;    &lt;header&gt;        &lt;form id=&quot;search-form&quot; class=&quot;nav-bar&quot;&gt;            &lt;h1 class=&quot;nav-logo&quot;&gt;                Maps Clone            &lt;/h1&gt;            &lt;input type=&quot;text&quot; class=&quot;search&quot; name=&quot;searchTerm&quot; required /&gt;            &lt;button type=&quot;submit&quot; class=&quot;submit-btn&quot;&gt;                Search                &lt;/button&gt;        &lt;/form&gt;    &lt;/header&gt;    &lt;div id=&quot;map&quot;&gt;&lt;/div&gt;&lt;/div&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Lastly, we need to add some basic styling. Create a file named “style.css” in &#x2F;static&#x2F;css. Then, import your style sheet, and paste in <a href="https://github.com/brandonbyr4/maps-clone/blob/main/static/css/style.css">these styles</a> from the <a href="https://github.com/brandonbyr4/maps-clone">Github reposory</a>.</p><p><strong>Adding the map</strong><br>We have an HTML div element with an id of map, but there is no actual map being rendered yet. To render the map, paste the CSS and script import for leaflet JS into the head of index.ejs.  </p><pre><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;https://unpkg.com/leaflet@1.9.3/dist/leaflet.css&quot; integrity=&quot;sha256-kLaT2GOSpHechhsozzB+flnD+zUyjE2LlfWPgU04xyI=&quot; crossorigin=&quot;&quot; /&gt;&lt;script src=&quot;https://unpkg.com/leaflet@1.9.3/dist/leaflet.js&quot; integrity=&quot;sha256-WBkoXOwTeyKclOHuWtc+i2uENFpDZ9YPdf5Hf+D7ewM=&quot; crossorigin=&quot;&quot;&gt;&lt;/script&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Last but not least, paste the following code into a new JavaScript file in &#x2F;static&#x2F;js&#x2F;maplogic.js.  </p><pre><code>var map = L.map(&#39;map&#39;).setView([33.659541, -118.1552947], 9)L.tileLayer(&#39;https://tile.openstreetmap.org/&#123;z&#125;/&#123;x&#125;/&#123;y&#125;.png&#39;, &#123;    maxZoom: 19,    attribution: &#39;&amp;copy; &lt;a href=&quot;http://www.openstreetmap.org/copyright&quot;&gt;OpenStreetMap&lt;/a&gt;&#39;&#125;).addTo(map);const updateMap = (coordinates) =&gt; &#123;    map.setView([coordinates[0], coordinates[2]], 9)&#125;document.getElementById(&quot;search-form&quot;).addEventListener(&#39;submit&#39;, (event) =&gt; &#123;    event.preventDefault()    const searchTerm = event.target.searchTerm.value    fetch((&quot;https://nominatim.openstreetmap.org/search?format=json&amp;limit=3&amp;q=&quot; + searchTerm), &#123;        method: &quot;GET&quot;,        headers: &#123;            &#39;Content-Type&#39;:&#39;application/json&#39;,        &#125;    &#125;).then((response) =&gt; response.json())    .then((responseJson) =&gt; &#123;        if(responseJson[0]) &#123;            const coordinates = responseJson[0].boundingbox            updateMap(coordinates)        &#125; else &#123;            alert(&quot;No locations found.&quot;)        &#125;    &#125;)    .catch((error) =&gt; &#123;        console.error(error)    &#125;)&#125;);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>This renders out the map using the Leaflet script we imported, as well as sets up an event listener to re-render the map when we search for a location.</p><p><strong>Conclusion</strong><br>I hope you enjoy this map project as much as I did making it. I think these open source map libraries have huge potential, and I hope you can get this project running locally to test them out. If you want to download the full source code for this project the repo can be found on Github <a href="https://github.com/Phantasm0009/Maps-Clone">here</a>.</p>]]></content>
      
      
      
        <tags>
            
            <tag> API </tag>
            
            <tag> Node.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Simple Search Box With Icon</title>
      <link href="/2022/12/17/search/"/>
      <url>/2022/12/17/search/</url>
      
        <content type="html"><![CDATA[<p>It’s like this:</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--W7qvCPMG--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/eyknz8xfm7e5d5qcag7t.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--W7qvCPMG--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/eyknz8xfm7e5d5qcag7t.png" alt="Image description"></a></p><p>Here is the code:  </p><pre><code>&lt;input class=&quot;search&quot; type=&quot;search&quot; placeholder=&quot;Search...&quot;&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><pre><code>    input.search &#123;        width: 260px;        border: 1px solid #555;        display: block;        padding: 9px 4px 9px 40px;        background: transparent url(&quot;/assets/search.svg&quot;) no-repeat 13px;    &#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p><code>padding</code> top, left, bottom, right - Places <code>placeholder</code> named <code>Search</code> just beside icon inside input.</p><p>download <code>svg</code> from any icon store say <a href="https://icons.getbootstrap.com/icons/search/">bootstrap</a> and place inside folder.</p><p><code>background</code> property - color, image, repeat, position - Places search icon in proper place in search box.</p><p>Thanks.</p>]]></content>
      
      
      
        <tags>
            
            <tag> Html </tag>
            
            <tag> Search </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>How to Build a Todo App With React and Firebase Database</title>
      <link href="/2022/12/16/todo/"/>
      <url>/2022/12/16/todo/</url>
      
        <content type="html"><![CDATA[<h2 id="How-to-build-a-todo-app-with-React-and-Firebase-Database-3-Part-Series"><a href="#How-to-build-a-todo-app-with-React-and-Firebase-Database-3-Part-Series" class="headerlink" title="How to build a todo app with React and Firebase Database (3 Part Series)"></a><a href="/rossanodan/series/1157">How to build a todo app with React and Firebase Database (3 Part Series)</a></h2><p><a href="/rossanodan/how-to-build-a-todo-app-with-react-and-firebase-database-1kik" title="Published Jun 11 &#39;19">1 How to build a todo app with React and Firebase Database</a> <a href="/rossanodan/how-to-build-a-todo-app-with-react-and-firebase-database-coh" title="Published Jun 11 &#39;19">2 How to build a todo app with React and Firebase Database</a> <a href="/rossanodan/how-to-build-a-todo-app-with-react-and-firebase-database-3gmh" title="Published Jun 28 &#39;19">3 How to build a todo app with React and Firebase Database</a></p><h2 id="Day-1"><a href="#Day-1" class="headerlink" title="Day 1"></a><a href="#day-1"></a>Day 1</h2><p>📅 11-06-2019<br>🕐 1h<br>🏁 Initial setup and getting ready</p><h1 id="Initial-setup"><a href="#Initial-setup" class="headerlink" title="Initial setup"></a><a href="#initial-setup"></a>Initial setup</h1><p>I’m going to use <code>create-react-app</code> tool to scaffold the project folder. It’s a tool provided by Facebook that allow to easy scaffold a pre-configured starter project.</p><pre><code>npx create-react-app todo-app</code></pre><p>The initial project consists of</p><ul><li><code>node_modules</code>: contains all necessary dependencies. It’s generated scaffolding the app with <code>create-react-app</code> tool (there’s a <code>npm install</code> into it)</li><li><code>public</code>: contains few files like the <code>index.html</code>, the <code>application favicon</code> and a <code>manifest</code> that contains few basic application settings</li><li><code>src</code>: contains the code</li><li><code>.gitignore</code></li><li><code>package.json</code>: there are all the project information like the version, the author and mainly the dependencies the application needs to work properly</li><li><code>yarn.lock</code>: contains all the dependencies Yarn needs with relative versions</li></ul><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--8RpZVUt0--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/ac9cpb4bpprx03cm8euv.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--8RpZVUt0--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/ac9cpb4bpprx03cm8euv.png"></a></p><p>To run the starter basic application it’s enough to do</p><pre><code>cd todo-appnpm start</code></pre><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--SAZJSz6j--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/r7rniixpbddwys3hx554.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--SAZJSz6j--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/r7rniixpbddwys3hx554.png"></a></p><p><code>npm start</code> is one of several pre-configured commands I’m going to use to develop this application. Other commands are:</p><ul><li><code>npm test</code></li><li><code>npm build</code></li><li><code>npm eject</code> (stay away from it for now)</li></ul><h1 id="Get-ready-for-components"><a href="#Get-ready-for-components" class="headerlink" title="Get ready for components"></a><a href="#get-ready-for-components"></a>Get ready for components</h1><p>In order to work with a sustainable and scalable structure, I like to keep things separated. I’m going to create two folders for components.</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--TvKQxUZr--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/ty6xjeagmabliffuwtvi.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--TvKQxUZr--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/ty6xjeagmabliffuwtvi.png"></a></p><p>These two folders will contains (surprise) components!<br>The only difference between them is that a <strong>container</strong> is a component that <strong>manages the application state</strong> so it’s a <strong>stateful component</strong>. Other components are <strong>stateless components</strong>.</p><h1 id="The-main-component-lt-App-gt"><a href="#The-main-component-lt-App-gt" class="headerlink" title="The main component &lt;App /&gt;"></a><a href="#the-main-component-raw-ltapp-gt-endraw-"></a>The main component <code>&lt;App /&gt;</code></h1><p>Let’s create the first component.<br>I’m going go to move the <code>App.js</code>, <code>App.test.js</code> and <code>App.css</code> into their own folder <code>./containers/App/</code>:</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--HAJq00-D--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/hm7jg5fkc8x2vifhr40h.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--HAJq00-D--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/hm7jg5fkc8x2vifhr40h.png"></a></p><pre><code>// App.jsimport React, &#123; Component &#125; from &#39;react&#39;;import &#39;./App.css&#39;;class App extends Component &#123;  render() &#123;    return (      &lt;div className=&quot;App&quot;&gt;        Placeholder      &lt;/div&gt;    );  &#125;&#125;export default App;/* App.css */.App &#123;  text-align: center;&#125;</code></pre><p>No changes to the <code>App.test.js</code> at the moment.</p><p>Update <code>index.js</code> - importing App component - because files location is changed and delete useless files like <code>logo.svg</code>.</p><h1 id="The-lt-Todo-gt-component"><a href="#The-lt-Todo-gt-component" class="headerlink" title="The &lt;Todo /&gt; component"></a><a href="#the-raw-lttodo-gt-endraw-component"></a>The <code>&lt;Todo /&gt;</code> component</h1><p>Let’s create the <code>&lt;Todo /&gt;</code> component into the <code>./components</code> folder. Create <code>Todo.js</code>, <code>Todo.test.js</code> and <code>Todo.css</code>.</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--cJb-pF1e--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/1z32qe9hv618920cfdwx.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--cJb-pF1e--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://thepracticaldev.s3.amazonaws.com/i/1z32qe9hv618920cfdwx.png"></a></p><pre><code>// Todo.jsimport React from &#39;react&#39;;import &#39;./Todo.css&#39;;const todo = () =&gt; (    &lt;div className=&quot;Todo&quot;&gt;        &lt;p&gt;Placeholder&lt;/p&gt;    &lt;/div&gt;)export default todo;/* Todo.css */.Todo &#123;&#125; /* Empty for now */</code></pre><p><code>Todo.test.js</code> is similar to <code>App.test.js</code>:</p><pre><code>import React from &#39;react&#39;;import ReactDOM from &#39;react-dom&#39;;import Todo from &#39;./Todo&#39;;it(&#39;renders without crashing&#39;, () =&gt; &#123;  const div = document.createElement(&#39;div&#39;);  ReactDOM.render(&lt;Todo /&gt;, div);  ReactDOM.unmountComponentAtNode(div);&#125;);</code></pre><p>Now I can use the <code>&lt;Todo /&gt;</code> component into the <code>&lt;App /&gt;</code> component, doing:</p><pre><code>import React, &#123; Component &#125; from &#39;react&#39;;import &#39;./App.css&#39;;import Todo from &#39;../../components/Todo/Todo&#39;;class App extends Component &#123;  render() &#123;    return (      &lt;div className=&quot;App&quot;&gt;        &lt;Todo /&gt;      &lt;/div&gt;    );  &#125;&#125;export default App;</code></pre><p>Check out the code here  </p><h2 id="rossanodan-x2F-todo-app"><a href="#rossanodan-x2F-todo-app" class="headerlink" title=" rossanodan &#x2F; todo-app"></a><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--qF2jUiUG--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://practicaldev-herokuapp-com.freetls.fastly.net/assets/github-logo-6a5bca60a4ebf959a6df7f08217acd07ac2bc285164fae041eacb8a148b1bab9.svg" alt="GitHub logo"> <a href="https://github.com/Phantasm0009">rossanodan</a> &#x2F; <a href="https://github.com/Phantasm0009/TodoList">todo-app</a></h2><h3 id="Simple-to-do-app-built-with-React"><a href="#Simple-to-do-app-built-with-React" class="headerlink" title="Simple to-do app built with React."></a>Simple to-do app built with React.</h3><p>This project was bootstrapped with <a href="https://github.com/facebook/create-react-app">Create React App</a>.</p><h2 id="How-to-run-locally"><a href="#How-to-run-locally" class="headerlink" title="How to run locally"></a>How to run locally</h2><pre><code>git clone https://github.com/Phantasm0009/TodoList.gitcd todo-appnpm installnpm start</code></pre><p><a href="https://github.com/Phantasm0009/TodoList">View on GitHub</a></p><hr>]]></content>
      
      
      
        <tags>
            
            <tag> React </tag>
            
            <tag> Firebase </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Building a Music Player in React</title>
      <link href="/2022/12/16/music/"/>
      <url>/2022/12/16/music/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/music.webp" alt="$cover"></p><h1 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a><a href="#introduction"></a>Introduction</h1><p>Whoever is learning React and wants to build a project with React. There are a variety of blogs and articles to guide a such projects for developers. I do go through those articles but one kind of project is always missing in them. The missing projects are the music player and video player. Both of the projects will give you the opportunity to handle audio and video. You will learn many things such as handling audio for playing and pausing audio.</p><p>Today, we are going to build a music player in React. We will look into the following topics:</p><ul><li>Setting up the environment</li><li>Playing&#x2F;Pausing audio</li><li>Handling time of audio(In term of current time and complete duration of audio)</li><li>Adding a range slider for the timeline of audio</li></ul><p>After completing the project, our music player will look like this.</p><p>GIF</p><p><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--sHfL5Oxg--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_66%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ai4g1rt3oc5y819inkje.gif" alt="React Music Player"></p><p>If this excites you then let’s get started.</p><h1 id="Prerequisite-and-Setting-up-the-Environment"><a href="#Prerequisite-and-Setting-up-the-Environment" class="headerlink" title="Prerequisite and Setting up the Environment"></a><a href="#prerequisite-and-setting-up-the-environment"></a>Prerequisite and Setting up the Environment</h1><p>I assume that you have knowledge about the following technologies as prerequisite:</p><ul><li>JavaScript</li><li>HTML&#x2F;CSS</li><li>Basic React</li></ul><p>The environment setup is easy. You should have <a href="https://nodejs.org/en/">node.js</a> pre-installed for running the node related command in the terminal.</p><p>Navigate to the directory in which you want to create the project. Now, run the terminal and enter the below command to install react project.  </p><pre><code>npx create-react-app react-music-player</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Remove all the boilerplate and unnecessary code. We are now good to go.</p><h1 id="Dependencies"><a href="#Dependencies" class="headerlink" title="Dependencies"></a><a href="#dependencies"></a>Dependencies</h1><p>We need to install the following libraries to our project:</p><ul><li><a href="https://www.npmjs.com/package/use-sound">use-sound</a>: This will handle the audio file. It will load, play, and pause the audio along with other features.</li></ul><p>Install with the below command:  </p><pre><code>npm i use-sound</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><ul><li><a href="https://react-icons.github.io/react-icons/">react-icons</a>: For adding icons of play, pause, next and previous into our player.</li></ul><p>Install it with the below command:  </p><pre><code>npm i react-icons</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><h1 id="Player-js"><a href="#Player-js" class="headerlink" title="Player.js"></a><a href="#playerjs"></a>Player.js</h1><p>Create a <code>component</code> directory in the <code>src</code> folder. Inside that create a component with the name <code>Player.js</code>. This component will be our music player.</p><h2 id="Imports"><a href="#Imports" class="headerlink" title="Imports"></a><a href="#imports"></a>Imports</h2><p>As per the library to import in the file. You can look at it here:  </p><pre><code>import &#123; useEffect, useState &#125; from &quot;react&quot;; import useSound from &quot;use-sound&quot;; // for handling the soundimport qala from &quot;../assets/qala.mp3&quot;; // importing the musicimport &#123; AiFillPlayCircle, AiFillPauseCircle &#125; from &quot;react-icons/ai&quot;; // icons for play and pauseimport &#123; BiSkipNext, BiSkipPrevious &#125; from &quot;react-icons/bi&quot;; // icons for next and previous trackimport &#123; IconContext &#125; from &quot;react-icons&quot;; // for customazing the icons</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>You can look into comments for the explanation of the imports.</p><h2 id="Playing-and-Pausing-the-audio"><a href="#Playing-and-Pausing-the-audio" class="headerlink" title="Playing and Pausing the audio"></a><a href="#playing-and-pausing-the-audio"></a>Playing and Pausing the audio</h2><p>Let’s implement the mandatory feature of the player which is playing and pausing the audio.</p><p>At the top, we have a <code>isPlaying</code> state for storing the current status of the player. This will be helpful in conditional rendering the play&#x2F;pause icon according the status of the player.  </p><pre><code>const [isPlaying, setIsPlaying] = useState(false);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>We need to initialize the <code>useSound</code> with audio. It will return the play, pause, duration, and sound method.  </p><pre><code>const [play, &#123; pause, duration, sound &#125;] = useSound(qala);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p><code>play</code> and <code>pause</code> is for playing and pausing the audio. <code>duration</code> is for the length of the track in milliseconds. <code>sound</code> will provide us with the <a href="https://github.com/goldfire/howler.js">howler.js</a> method for the sound.</p><p>Create a function for handling the play and pause buttons. Here is the code for it.  </p><pre><code>  const playingButton = () =&gt; &#123;    if (isPlaying) &#123;      pause(); // this will pause the audio      setIsPlaying(false);    &#125; else &#123;      play(); // this will play the audio      setIsPlaying(true);    &#125;  &#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now, it’s time to add the UI component of the player into the <code>return</code>. Here is the code for it.  </p><pre><code>  return (    &lt;div className=&quot;component&quot;&gt;      &lt;h2&gt;Playing Now&lt;/h2&gt;      &lt;img        className=&quot;musicCover&quot;        src=&quot;https://picsum.photos/200/200&quot;      /&gt;      &lt;div&gt;        &lt;h3 className=&quot;title&quot;&gt;Rubaiyyan&lt;/h3&gt;        &lt;p className=&quot;subTitle&quot;&gt;Qala&lt;/p&gt;      &lt;/div&gt;      &lt;div&gt;        &lt;button className=&quot;playButton&quot;&gt;          &lt;IconContext.Provider value=&#123;&#123; size: "3em", color: "#27AE60" &#125;&#125;&gt;            &lt;BiSkipPrevious /&gt;          &lt;/IconContext.Provider&gt;        &lt;/button&gt;        &#123;!isPlaying ? (          &lt;button className=&quot;playButton&quot; onClick=&#123;playingButton&#125;&gt;            &lt;IconContext.Provider value=&#123;&#123; size: "3em", color: "#27AE60" &#125;&#125;&gt;              &lt;AiFillPlayCircle /&gt;            &lt;/IconContext.Provider&gt;          &lt;/button&gt;        ) : (          &lt;button className=&quot;playButton&quot; onClick=&#123;playingButton&#125;&gt;            &lt;IconContext.Provider value=&#123;&#123; size: "3em", color: "#27AE60" &#125;&#125;&gt;              &lt;AiFillPauseCircle /&gt;            &lt;/IconContext.Provider&gt;          &lt;/button&gt;        )&#125;        &lt;button className=&quot;playButton&quot;&gt;          &lt;IconContext.Provider value=&#123;&#123; size: "3em", color: "#27AE60" &#125;&#125;&gt;            &lt;BiSkipNext /&gt;          &lt;/IconContext.Provider&gt;        &lt;/button&gt;      &lt;/div&gt;    &lt;/div&gt;  );</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>For the cover image, I have used the <a href="https://picsum.photos/">Loren Picsum</a> for generating a random image.</p><p>You can look here for the CSS of the file:  </p><pre><code>body &#123;  background-color: #e5e5e5;&#125;.App &#123;  font-family: sans-serif;  text-align: center;&#125;.component &#123;  background-color: white;  width: 25%;  max-width: 600px;  margin: 1em auto;  padding-bottom: 2em;  border: 0.1px solid black;  border-radius: 10px;&#125;.musicCover &#123;  border-radius: 10%;&#125;.playButton &#123;  background: none;  border: none;  align-items: center;  justify-content: center;&#125;.subTitle &#123;  margin-top: -1em;  color: #4f4f4f;&#125;.time &#123;  margin: 0 auto;  width: 80%;  display: flex;  justify-content: space-between;  color: #828282;  font-size: smaller;&#125;.timeline &#123;  width: 80%;  background-color: #27ae60;&#125;input[type=&quot;range&quot;] &#123;  background-color: #27ae60;&#125;@media (max-width: 900px) &#123;  .component &#123;    width: 50%;  &#125;&#125;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Run the react server. If everything goes well you will be able to see the below screen.</p><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--DL94VPQb--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/vi5q1apc13fsx3pcn0cp.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--DL94VPQb--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/vi5q1apc13fsx3pcn0cp.png" alt="Screenshot - React Music Player"></a></p><p>Click on the play button to play the audio.</p><h2 id="Adding-audio-timeline-with-the-current-time-and-duration-of-the-audio"><a href="#Adding-audio-timeline-with-the-current-time-and-duration-of-the-audio" class="headerlink" title="Adding audio timeline with the current time and duration of the audio"></a><a href="#adding-audio-timeline-with-the-current-time-and-duration-of-the-audio"></a>Adding audio timeline with the current time and duration of the audio</h2><p>Now, let’s add the timeline to the player. The timeline will controllable by the user. Any changes to it will change the audio’s current position.</p><p>Let’s look into the state that we are using. You see the comments for an explanation of each state.  </p><pre><code>  const [currTime, setCurrTime] = useState(&#123;    min: &quot;&quot;,    sec: &quot;&quot;,  &#125;); // current position of the audio in minutes and seconds  const [seconds, setSeconds] = useState(); // current position of the audio in seconds</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>We are grabbing the duration props from the <code>useSound</code>. As the duration is provided in milliseconds, we have converted it into minutes and seconds.  </p><pre><code>useEffect(() =&gt; &#123;  const sec = duration / 1000;  const min = Math.floor(sec / 60);  const secRemain = Math.floor(sec % 60);  const time = &#123;    min: min,    sec: secRemain  &#125;;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now, for the current position of the audio, we have the <code>sound.seek([])</code> method. We are running this function every second to change the current position of the audio. After getting the position of the audio which is in seconds. We are converting it into minutes and seconds. After converting, we are setting the state with the current value. Here is the code for it.  </p><pre><code>useEffect(() =&gt; &#123;    const interval = setInterval(() =&gt; &#123;      if (sound) &#123;        setSeconds(sound.seek([])); // setting the seconds state with the current state        const min = Math.floor(sound.seek([]) / 60);        const sec = Math.floor(sound.seek([]) % 60);        setCurrTime(&#123;          min,          sec,        &#125;);      &#125;    &#125;, 1000);    return () =&gt; clearInterval(interval);  &#125;, [sound]);</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>Now for the return. Here is the code.  </p><pre><code>      &lt;div&gt;        &lt;div className=&quot;time&quot;&gt;          &lt;p&gt;            &#123;currTime.min&#125;:&#123;currTime.sec&#125;          &lt;/p&gt;          &lt;p&gt;            &#123;time.min&#125;:&#123;time.sec&#125;          &lt;/p&gt;        &lt;/div&gt;        &lt;input          type=&quot;range&quot;          min=&quot;0&quot;          max=&#123;duration / 1000&#125;          default=&quot;0&quot;          value=&#123;seconds&#125;          className=&quot;timeline&quot;          onChange=&#123;(e) =&gt; &#123;            sound.seek([e.target.value]);          &#125;&#125;        /&gt;      &lt;/div&gt;</code></pre><p>Enter fullscreen mode Exit fullscreen mode</p><p>The value of the range input is the <code>second</code> state. It will provide us with the current position of the audio. On change in the range by the user. We are calling the <code>soud.seek()</code> method to change the current position of the audio.</p><h1 id="Output"><a href="#Output" class="headerlink" title="Output"></a><a href="#output"></a>Output</h1><p>After successfully completing the project, you will be able to see the below output.</p><p>GIF</p><p><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--sHfL5Oxg--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_66%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ai4g1rt3oc5y819inkje.gif" alt="Image description"></p><blockquote><p>Note: The music is coming into my speaker.</p></blockquote><p>I have created a codesandbox. You can look at it for the complete project with code and the output.</p><blockquote><p>Note: The song that I used is <a href="https://www.youtube.com/watch?v=wEkMPeP50Ss">Rubaaiyaan</a> from Qala. All credit to the creator.</p></blockquote><h1 id="Additional-Features"><a href="#Additional-Features" class="headerlink" title="Additional Features"></a><a href="#additional-features"></a>Additional Features</h1><p>You can work on the music player to add more features such as:</p><ul><li>It currently plays one song and loads many songs into it. Use the next and the previous icons for changing audio.</li><li>Change the name and album of the audio as per the song.</li><li>Add more features that you wish a music player should have.</li></ul><h1 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a><a href="#conclusion"></a>Conclusion</h1><p>We have created our own music player. This project will help you in handling audio files in React. We have added features of play and pause with the function. Also added an audio timeline with the range input. Users can change the current position of the audio with the timeline. Feel free to add more features to the project.</p><p>I hope this project has helped you in understanding a method of handling music in React. Let me know in the comment if you want a video player tutorial too. Thanks for reading the blog post.</p>]]></content>
      
      
      
        <tags>
            
            <tag> React </tag>
            
            <tag> Javascript </tag>
            
            <tag> Html </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>VueJS vs ReactJS</title>
      <link href="/2022/12/16/vue-js/"/>
      <url>/2022/12/16/vue-js/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/vue.webp" alt="$cover"><br><strong>VueJS and ReactJS</strong> are both popular JavaScript libraries for building user interfaces. They are both open-source and are maintained by a large community of developers.</p><p><strong>Here are some key differences between the two:</strong></p><p>1)VueJS is a progressive framework, which means it can be used for building small, simple apps or large, complex ones. React, on the other hand, is a declarative library, which is better suited for building large-scale applications.</p><p>2)VueJS uses a component-based architecture, which makes it easy to reuse components and build large, scalable applications. React, on the other hand, uses a virtual DOM (Document Object Model) that allows for efficient updates and rendering of components.</p><p>3)VueJS has a more straightforward and easier to learn syntax, which makes it a good choice for developers who are new to JavaScript. React, on the other hand, has a steep learning curve and requires more advanced knowledge of JavaScript.</p><p>ALso,VueJS and ReactJS are both popular JavaScript libraries for building user interfaces. They are both open-source and maintained by Facebook.</p><p>VueJS was created by Evan You, a former Google employee, in 2014. It is a progressive framework, which means it can be used to build both small and large-scale applications. It is known for its simplicity, flexibility, and intuitive API.</p><p>ReactJS was created by Facebook in 2013. It is a declarative, efficient, and flexible JavaScript library for building user interfaces. It is used by many large companies, including Facebook, Netflix, and Airbnb.</p><p>When it comes to choosing between VueJS and ReactJS, it ultimately comes down to personal preference and the specific needs of your project. Both libraries have their own strengths and weaknesses, and both are widely used and well-supported by a large and active community.</p><p>If you’re just starting out with JavaScript and are looking for a framework that is easy to learn and use, VueJS may be a good choice. If you’re working on a large-scale project with a lot of data, ReactJS may be a better option because of its performance and scalability. Ultimately, the best choice will depend on your specific needs and requirements.</p>]]></content>
      
      
      
        <tags>
            
            <tag> React </tag>
            
            <tag> Vue.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>How to Build a Piano With Only HTML, CSS, Javascript ?</title>
      <link href="/2022/12/15/Piano/"/>
      <url>/2022/12/15/Piano/</url>
      
        <content type="html"><![CDATA[<p><img src="/images/code.webp" alt="$cover"></p><p>In this post, we’ll learn how to build <a href="https://javascript-piano-lotfijb.netlify.app/">this piano</a> from scratch using simple HTML, CSS, and JavaScript.</p><p>Firstly, we’ll structure our web app using HTML. We’ll use the main tag to make a wrapper structure for the piano.</p><p>We will create classes to show white and black keys, and classes will be further used in CSS for designing.</p><p>I added a class empty to some black keys because we don’t need the same amount of white keys and I tried to do it the simple way.</p><p>In script.js, I grabbed all keys with className to make an array of all keys and created an array of sounds.</p><p>I, then, iterated through every single element of array keys and added a click event to play a random sound.</p><p>I, also, created a play() function to play a random sound on every keyboard button press on body element (I don’t recommend that but I wanted to make it simple).</p><p>I created a sounds folder where I stored 21 different sounds of piano keys.</p><h2 id="index-html"><a href="#index-html" class="headerlink" title="index.html"></a><a href="#indexhtml"></a>index.html</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--AqHrw2rS--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/x8qju1wrknh426chh6en.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--AqHrw2rS--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/x8qju1wrknh426chh6en.png" alt="HTML"></a></p><h2 id="style-css-click-to-zoom"><a href="#style-css-click-to-zoom" class="headerlink" title="style.css ( click to zoom )"></a><a href="#stylecss-click-to-zoom-"></a>style.css ( click to zoom )</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--0f-_9Uhq--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/9196a4jg6rna5c89r9e8.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--0f-_9Uhq--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/9196a4jg6rna5c89r9e8.png" alt="CSS"></a></p><h2 id="script-js"><a href="#script-js" class="headerlink" title="script.js"></a><a href="#scriptjs"></a>script.js</h2><p><a href="https://res.cloudinary.com/practicaldev/image/fetch/s--fY-2ba_Z--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ptfpfqkuaiamqrsjukb6.png"><img src="https://res.cloudinary.com/practicaldev/image/fetch/s--fY-2ba_Z--/c_limit%2Cf_auto%2Cfl_progressive%2Cq_auto%2Cw_880/https://dev-to-uploads.s3.amazonaws.com/uploads/articles/ptfpfqkuaiamqrsjukb6.png" alt="JS"></a></p><p>NOTE :<br>There is room for improvement and this is only for beginners who just started as it covers simple concepts.</p>]]></content>
      
      
      
        <tags>
            
            <tag> typography </tag>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
